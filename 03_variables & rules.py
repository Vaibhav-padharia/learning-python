# Variables can be considered as a container that holds some value

a=25
b=35
print(a+b)

# Here we have defined variables a and b and assigned them some values 
# now if we add variables a and b we get their combined output as 60 
# We can even print strings in variables for example

firstname="jay"
secondname="padhraia"
print (firstname + secondname)

#There are some specific rules that are to follwed while defining a variable
print("A variable name can only contain letters, numbers, or underscores")
print("A variable name cant start with a number")
print(" No while space are allowed to be used inside a variable name")
print("A variable name can only start with a letter or an underscore")
