# A set is a collection of well defined objects
# we can declare it to be a set using the set()
# because both the dictionary and set have the same syntax

a={"jay","hidy","hrht"}
a=set()
print(type(a))
